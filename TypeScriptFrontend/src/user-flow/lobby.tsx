import React from "react";

const Lobby: React.FC = () => {
    return (
        <div>
            <h1>Waiting...</h1>
            {/* Add your waiting symbol here */}
        </div>
    );
};

export default Lobby;
